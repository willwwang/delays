trips:
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: delays-486122
      dataset: delays # You can also use "schema" here
      threads: 1 # Must be a value of 1 or greater 

    prod:
      type: bigquery
      method: oauth
      project: delays-486122
      dataset: delays # You can also use "schema" here
      threads: 1 # Must be a value of 1 or greater 

  target: dev
